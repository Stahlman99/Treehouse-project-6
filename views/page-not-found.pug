extends layout.pug

block content
    nav.grid-container.portfolio-breadcrumb
        a(href='/') ← Back
    article.grid-container.portfolio-intro

      h1 Uh oh. It looks like that page does not exist.

      h1 See details:

      br
      p.lead.text-light
        b Error status: 
        | #{error.status}
        br
        b Error message: 
        | #{error.message}